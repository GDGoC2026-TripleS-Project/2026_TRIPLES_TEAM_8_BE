spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Gread?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: Qepoal1379@!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false

jwt:
  secret: ${JWT_SECRET:default_secret_key_1234567890}
  access-token-validity-in-milliseconds: ${ACCESS_TOKEN_VALIDITY_IN_MILLISECONDS:3600000}